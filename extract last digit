#include <stdio.h>

int main() {
    int number, last_digit;

    printf("Enter a number: ");
    scanf("%d", &number);

    last_digit = number % 10;

    printf("Last digit = %d\n", last_digit);

    return 0;
}
